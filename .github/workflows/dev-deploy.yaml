name: Deploy-to-dev

on:
  push:
    branches:
      - dev
jobs:
  Build-and-Deploy:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: build
        run: apt install nodejs && node -v

